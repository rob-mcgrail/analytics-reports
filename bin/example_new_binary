require File.expand_path(File.dirname(__FILE__) + "/../lib/analytics.rb")

$display = Unix.new
$periods = Periods.new

interface = Startup.new

interface.authenticate_session
interface.arbitrary_periods

$format = XML.new

report = Biotech.new
report.name = "Biotech"

$display.tell_user("\nRunning the biotech hub report\n")

puts report
puts "\n\n"

report.session_startup    #just because this is a one site only report
report.header
report.main
report.end_game
